#pragma once
#include <Arduino.h>

/**
 * @brief Estado de dirección del robot.
 * Representa las direcciones posibles del movimiento del robot.
 */
// Note: Currently using String for direction state.
// Could be refactored to enum in the future for better type safety:
// enum class Direction { CENTER, ARRIBA, ABAJO, IZQUIERDA, DERECHA };
